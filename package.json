{
  "name": "iqtoolkit-analyzer-dev",
  "version": "0.2.3a1",
  "private": true,
  "description": "Development dependencies for Iqtoolkit Analyzer",
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0"
  },
  "scripts": {
    "commitlint": "commitlint",
    "commitlint:check": "commitlint --from HEAD~1 --to HEAD --verbose"
  }
}